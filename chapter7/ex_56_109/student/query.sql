SELECT 
    C.PAT_ID,
    C.BOOK_NUM,
    P.PAT_FNAME,
    P.PAT_LNAME,
    B.BOOK_TITLE
FROM 
    CHECKOUT C
JOIN PATRON P ON C.PAT_ID = P.PAT_ID
JOIN BOOK B ON C.BOOK_NUM = B.BOOK_NUM
WHERE 
    C.CHECK_IN_DATE IS NULL
ORDER BY 
    P.PAT_LNAME,
    B.BOOK_TITLE;