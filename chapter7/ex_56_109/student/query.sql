SELECT 
    PAT_ID AS `PATRON`,
    BOOK_NUM AS `BOOK`,
    CHECK_IN_DATE - CHECK_OUT_DATE AS `Days Kept`
FROM 
    CHECKOUT
WHERE 
    CHECK_IN_DATE IS NOT NULL
ORDER BY 
    `Days Kept` DESC,
    PAT_ID ASC,
    BOOK_NUM ASC;