SELECT 
    PAT_ID AS Patron,
    BOOK_NUM AS Book,
    DATEDIFF(CHECK_DUE_DATE, CHECK_OUT_DATE) AS `Days Kept`
FROM 
    CHECKOUT
ORDER BY 
    DATEDIFF(CHECK_DUE_DATE, CHECK_OUT_DATE) DESC,
    PAT_ID,
    BOOK_NUM;